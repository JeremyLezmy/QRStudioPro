name: Desktop Build And Release

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            label: windows
          - os: macos-latest
            label: macos
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: python -m pip install --upgrade pip && pip install -r requirements.txt

      - name: Build Windows app
        if: matrix.label == 'windows'
        shell: pwsh
        run: ./scripts/build_windows.ps1

      - name: Build macOS app
        if: matrix.label == 'macos'
        shell: bash
        run: bash ./scripts/build_macos.sh

      - name: Archive Windows bundle
        if: matrix.label == 'windows'
        shell: pwsh
        run: Compress-Archive -Path dist/QRStudioPro -DestinationPath QRStudioPro-windows.zip

      - name: Archive macOS bundle
        if: matrix.label == 'macos'
        shell: bash
        run: ditto -c -k --sequesterRsrc --keepParent dist/QRStudioPro.app QRStudioPro-macos.zip

      - name: Upload Windows artifact
        if: matrix.label == 'windows'
        uses: actions/upload-artifact@v4
        with:
          name: windows-bundle
          path: QRStudioPro-windows.zip

      - name: Upload macOS artifact
        if: matrix.label == 'macos'
        uses: actions/upload-artifact@v4
        with:
          name: macos-bundle
          path: QRStudioPro-macos.zip

  release:
    if: startsWith(github.ref, 'refs/tags/')
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: windows-bundle
          path: artifacts/windows

      - name: Download macOS artifact
        uses: actions/download-artifact@v4
        with:
          name: macos-bundle
          path: artifacts/macos

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/windows/QRStudioPro-windows.zip
            artifacts/macos/QRStudioPro-macos.zip
          generate_release_notes: true
